- handle tab character
- handle non printable characters
- sometimes keys are lost
- cursor down doesn't work on Fedora21 VM
- don't go further down than last line of file
- if a line covers multiple rows, page down needs to only advance the lines displayed and not the complete line window height

- modify how lines are displayed with regex
- display lines with curses
